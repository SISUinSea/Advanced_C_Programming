#include <stdio.h>
#include <string.h>

int main() {
    struct student {
        int id;
        char name[8];
        double grade;
    };

    struct student ast[3];
    ast[0].id = 10;
    strcpy(str[0].name, "Tom");
    ast[0].grade = 3.2;
    return 0;
}