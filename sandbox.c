// #include <stdio.h>

// int main() {
    
//     return 0;
// }

#include <stdio.h>
#include <string.h>

int main() {
    char a[21], b[21], tmp[61];
    scanf("%s", a);
    // getchar();
    scanf("%s", b);

    printf("%s\n%s", a, b);

    // printf("%d %d\n",(int) strlen(a),(int) strlen(b));
    // strcmp(a, b) < 0 ? printf("%s\n",a) : printf("%s\n", b);


    // strcpy(tmp, a);
    // strcat(tmp, b);
    // strcat(tmp, a);

    // printf("%s\n", tmp);
    return 0;
}